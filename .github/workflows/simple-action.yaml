name: Simple Action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Simple JS Action
      uses: action/hello-world-javascript-action@v1
      with:
        who-to-greet: 'Mohit the Singh'
        greeting: 'Hello'